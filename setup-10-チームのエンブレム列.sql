-- ============================================================
-- 大会ベース：大会エントリー（gn_teams）にエンブレム列を追加（2026-08-20）
--
-- 目的：
--   公式記録のチーム名欄に、台帳に登録した校章・エンブレムを
--   チーム名と一緒に表示できるようにする。
--   台帳（gn_clubs.crest）は年度をまたいで変わることがあるため、
--   大会にチームを入れた時点の画像を gn_teams 側にもコピーして持つ
--   （選手名簿(players)を大会エントリー時にコピーしているのと同じ考え方）。
--
-- 実行しても壊れないか：
--   列を1つ追加するだけ（NULL許容）。既存データへの影響なし。
-- ============================================================

alter table public.gn_teams add column if not exists crest text;

-- index.htmlの対応する変更もあわせて反映してください。
